# Development & CI tooling (linters, type checks, security).
# Runtime/test deps live in requirements.txt.

# Python linters and security.
# Pinned to match the local venv (/Users/lz/dev/.venv) so CI and local runs
# produce identical formatting/lint results — a newer black/ruff can change
# style and turn the build red unexpectedly.
ruff==0.12.10
black==26.3.1
mypy==1.17.1
bandit==1.8.6

# Cyclomatic-complexity ratchet (xenon wraps radon; see `make complexity-py`).
radon==6.0.1
xenon==0.9.3

# Dependency-structure gate (import-linter wraps grimp; see `make imports-py`).
import-linter==2.13

# Type stubs
types-requests

# Include runtime + test deps
-r requirements.txt
