# Stats Page Customizer

This add-on scaffolds future tweaks to Anki's stats page. Extend `__init__.py`
with custom logic that hooks into `gui_hooks.stats_dialog_will_show` to adjust
the rendered content.
