# Local notes (not part of the vendored AnKing addon)

## Hide the deckbrowser horizontal scrollbar

`user_files/css/custom_deckbrowser.css` already sets:

```css
body {
  overflow-x: hidden;
  overflow-y: hidden;
}
```

This removes the scrollbar Anki otherwise shows on the main deck list window.
